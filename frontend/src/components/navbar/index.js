import React from 'react';

const NavBar = () => (
    <nav>
        <div className="nav-wrapper purple darken-4">
        <a href="/" className="brand-logo center">Welcome, Here you can Add, Edit, Delete Users in the list</a>
        </div>
    </nav>
);

export default NavBar;